FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (ignoring dev dependencies for production backend)
RUN npm install --omit=dev

# Copy backend source code
COPY server/ ./server/
COPY server.js ./

# Set environment to production
ENV NODE_ENV=production

EXPOSE 5000

CMD ["npm", "start"]
